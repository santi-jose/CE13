/*Module containing implementations of
 *Add two numbers and Increment by one*/
#ifndef SIMPLEMATH_H
#define SIMPLEMATH_H

/*SquarePlusOne squares x, adds one, and returns the result
 *x is not changed
 */
int SquarePlusOne(int x);

/*DoubleLargest finds the larger of x and y,
 * and returns that number
 * Neither x or y is changed
 */
int DoubleLargest(int y, int x);

#endif